# feat: Rules CRUD + Engram V10 + Lean Reminder (v4.0.0)

**Statut:** IMPLEMENTED (2026-02-28)
**Version:** v3.9.0-alpha → v4.0.0-alpha

## Context

Trois problèmes :
1. **Rules garbage** : hook inject pousse ~600 tokens/msg de fragments bruts auto-détectés. `ai_profile` = stub hardcodé.
2. **Engram opaque** : l'agent ne voit ni les thread_id, ni weight/confidence/importance des threads injectés. Impossible d'agir dessus (ai_recall, ai_rate_importance, etc.)
3. **Engram incomplet** : V8 FocusAlignment mort (`load_focus_topics` = stub vide). Pas de pénalisation des threads tronqués.

## Plan — 8 Steps

### Step 1: Désactiver `detect_rules()` → retourne `None`

**Fichier** : `src/user_profile.rs`

```rust
pub fn detect_rules(&mut self, _message: &str) -> Option<String> {
    None  // Explicitly disabled — was capturing garbage
}
```

### Step 2: Rules CRUD dans `handle_profile()`

**Fichier** : `src/mcp/tools/status.rs`

Actions implémentées : `view`, `set_rule`, `remove_rule`, `list`, `clear_rules`

- `set_rule` : param `value` → `profile.add_rule(value)` + save
- `remove_rule` : param `key` (1-based index) → `profile.remove_rule(idx)` + save
- `clear_rules` : `profile.clear_rules()` + save
- `view` : retourne identity + preferences + rules avec index
- `list` : retourne rules avec index + count

### Step 3: `build_injection()` — numbered rules format

**Fichier** : `src/user_profile.rs`

Format d'injection Layer 5.5 :
```
User profile: developer, expert level
Custom sections: use ai_pin for persistent reminders
Rules (ai_profile set_rule / remove_rule):
1. Always bump version BEFORE build
2. Never modify LLM prompts without explicit user authorization
```

### Step 4: V10 TruncationPenaltyValidator + `has_truncated_origin`

**Fichiers** :
- `src/thread.rs` — nouveau champ `has_truncated_origin: bool`
- `src/intelligence/validators/mod.rs` — V10 TruncationPenaltyValidator
- `src/storage/threads.rs` — `has_truncated_origin` dans SQL SELECT + `row_to_thread`
- `src/storage/migrations.rs` — colonne `has_truncated_origin INTEGER DEFAULT 0`
- `src/config.rs` — weights vector 9 → 10 éléments, thresholds updated

V10 pénalise les threads dont l'origine a été tronquée (capture incomplète).
Quorum : StrongInject ≥ 5/10, WeakInject ≥ 3/10.

### Step 5: `ScoredThread` return type

**Fichier** : `src/intelligence/engram_retriever.rs`

```rust
#[derive(Debug, Clone)]
pub struct ScoredThread {
    pub thread: Thread,
    pub weighted_score: f64,
    pub pass_count: u8,
    pub decision: InjectionDecision,
}
```

`get_relevant_context()` retourne `Vec<ScoredThread>` au lieu de `Vec<Thread>`.
`reinforce_used_bridges` signature : `&[Thread]` → `&[&Thread]`.

### Step 6: Lean reminder — `build_memory_context()` redesign

**Fichier** : `src/hook/inject.rs`

Nouvelle signature : `build_memory_context(conn, message, agent_quota, shared_threads_cache)`

Format compact :
```
Be proactive on your memory, dont wait to be requested about it. ai_help

Engram Threads (ai_recall <thread_id>):
{id} : "{title}" w:{weight} c:{score} i:{importance}

Shared threads (ai_discover / ai_subscribe):
{id} : "{title}" from {owner} ({role})
```

Helper `truncate_title()` pour limiter les titres à 40 chars.

### Step 7: Shared threads cache enrichi

**Fichier** : `src/mcp/server.rs`

Heartbeat H2 enrichi avec le rôle de l'agent depuis le registry :
```rust
let role = registry_conn.as_ref()
    .and_then(|rc| AgentRegistry::get(rc, &s.owner_agent, project_hash)
        .ok().flatten().map(|a| a.role))
    .unwrap_or_else(|| "agent".to_string());
serde_json::json!({
    "id": s.shared_id, "title": s.title,
    "from": s.owner_agent, "role": role, "topics": s.topics,
})
```

### Step 8: Bump + build + tests

- Bump 3.9.0-alpha → 4.0.0-alpha
- Build clean (2 warnings pré-existants pool_writer.rs)
- 205 tests pass, 0 failed

## Fichiers modifiés

| Fichier | Change |
|---------|--------|
| `src/user_profile.rs` | `detect_rules()` disabled, `build_injection()` restored |
| `src/mcp/tools/status.rs` | `handle_profile()` — full CRUD |
| `src/thread.rs` | `has_truncated_origin: bool` field |
| `src/intelligence/validators/mod.rs` | V10 TruncationPenaltyValidator |
| `src/intelligence/engram_retriever.rs` | `ScoredThread`, 10 validators |
| `src/hook/inject.rs` | `build_memory_context()` lean format |
| `src/mcp/server.rs` | Shared cache + agent role |
| `src/storage/threads.rs` | `has_truncated_origin` in SQL + row_to_thread |
| `src/storage/migrations.rs` | Column `has_truncated_origin` |
| `src/config.rs` | 10-element weights, updated thresholds |
| `src/mcp/tools/focus.rs` | `has_truncated_origin: false` (2 places) |
| `src/mcp/tools/split.rs` | `has_truncated_origin: false` |
| `src/mcp/tools/threads.rs` | `has_truncated_origin: false` |
| `src/healthguard/mod.rs` | `has_truncated_origin: false` (test) |
| `Cargo.toml` | Bump 4.0.0-alpha |
